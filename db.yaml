services: 
  s1db:
    image: postgres:latest
    container_name: 68-s1-database
    ports:
      - ${postgres_PORT}:5432
    environment:
      POSTGRES_USER: ${postgres_USER}
      POSTGRES_DB: ${postgres_NAME}
      POSTGRES_PASSWORD: ${postgres_PORT}